// examples/demo.ltl — runnable demo for ltl_regex_vm

import std::io
use ltl_regex_vm as lib

fn main() {
    io::println("ltl_regex_vm demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}